div
	button.btn.btn-default(ng-click='User.create()')
		| create user

	ul
		li(ng-repeat='user in data.users')
			button.btn.btn-default(ng-click='User.remove(user)')
				| 삭제
			button.btn.btn-default(ng-click='User.addNote(user)')
				| 노트추가
			h2
				| {{user.id}}
				ul
					li(ng-repeat='note in user.notes')
						button.btn.btn-default(ng-click='User.removeNote(user, note)')
							| 삭제
						button.btn.btn-default(ng-click='User.editNote(user, note)')
							| 수정
						h3
							| {{note.title}}

			pre {{user | json}}
